export class Merchants{
    public data: [
        {
            id: string,
            name: string,
            address: string,
            slug: string
        }
    ];
    public httpCode: number;
    public errorMessage: string;
}